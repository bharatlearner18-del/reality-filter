# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A **validation-first MVP** for "Reality Filter" — a hype-vs-reality check for AI launches. A user pastes an AI launch/tool/topic and gets a verdict (`worth trying now` · `watch later` · `too early` · `mostly fog machine`) backed by an evidence packet. This is a demand test, **not** the real product — there is intentionally no backend, database, or build system yet.

The two markdown files are the product's source of truth, written as pre-PRD specs:
- `research-brief.md` — the decided model, MVP scope, validation plan, and open questions.
- `deep-research-report.md` — market/moat analysis behind the brief.

Read these before changing product behavior. Key locked decisions from them:
- **Scope is locked to AI launches only** (Option A, confirmed with the user). It is explicitly NOT a competitor tracker, strategy advisor, or daily news feed. Do not broaden the subject to "any company/tool/news." (Landing-copy note, 2026-07-20: the user explicitly asked the landing page to speak to tracking "any topic — AI, startups, companies" and to competitor-awareness use cases for founders/PMs. The copy does that, but the *mechanism* stays hype-vs-reality verdicts on launches, and packets/data remain AI-launch-only.)
- **Do NOT build the backend/automation yet.** Building it before real users show up is the documented trap. Concierge/manual delivery is the plan until demand + willingness-to-track are proven.
- The moat is the **public dated track record** + the **Premium tracking workflow**, not the research itself (ChatGPT/Perplexity already do ~80% of the raw research).

## The app

Static files, no framework/bundler/dependencies (Google Fonts via CDN):
- `index.html` — **the main page**, redesigned 2026-07-20 into a **tracking-first landing** (the user's explicit repositioning: explain-the-service first, checker demoted). Section order: **hero `.hero-track`** (keeps `id="premium"` so `index.html#premium` links from the other two pages still land on the signup) — a two-column split, pitch + `.ht-points` proof list on the left, the **`.ht-card` signup card** on the right (2026-07-22: the `.lede` and `.ht-points` items were rewritten from what-it-does copy to benefit-led copy — each `<li>` now has a bold payoff lead-in (`<b>`) + an em-dash reason, e.g. "**Decide faster, and be right more often** — build, buy or ignore, called on evidence instead of a headline"; this forced `.ht-points li` off `display:flex` — flex was splitting the `<b>` and trailing text node into two columns — to `position:relative` + an absolutely-positioned `::before` checkmark instead; keep new bullets in that `<b>lead-in</b> — reason` shape, not flex-friendly short phrases) (step 1 `#wlTopics` topic chips incl. companies, step 2 `#wlForm` email, `.free-badge` "Free for the next 6 days", capped-at-15-trackers note) → **`#how`** (numbered `.how-steps` with `.src-pill` sources row + the `#pDemo` clip, sticky on desktop) → **`.inbox`** (what lands in your inbox) → **`.for-who`** (Founders / PMs / Builders cards) → **`#check`** — the original search console + `#result`, demoted but fully functional. An earlier rule said "the hero must be stacked, not split" — that applied to the search-bar-as-right-column layout it replaced; it does NOT forbid this split, where the right column is the page's one action card and the left is passive reading. Nav: How it works (`#how`) · Check a launch (`#check`, `#navCheck` scrolls to and focuses the search input) · Track record. (2026-07-23: Feedback was removed site-wide — nav link, footer `.fb-row` CTA, and the `feedback.html` page itself all deleted, per user request; don't re-add without being asked.) **No pricing language anywhere** — "Premium" naming was removed site-wide in the free-for-6-days repositioning; don't reintroduce it (or paid-plan talk) without being asked. Reads `?q=<launch>` from the URL and auto-runs a check (this is how track-record rows deep-link into a verdict; they land on the `#check` section's result area).
- `track-record.html` — the **public dated ledger** on its own page (it grows over time, so it lives apart from the tool). Each row links to `index.html?q=<name>` to open that verdict. Above the ledger sits `#trStats`, a scoreboard counted from the packets themselves (checked / held up as real / caveats / fog). It buckets by `verdictClass`, so **the class names there must stay in sync with `packets.js`** — `v-go` counts as "held up as real", `v-watch` and `v-early` as "caveats / too early", `v-fog` as "called out as fog". The numbers animate up via `requestAnimationFrame`; they read 0 in a background tab because rAF is paused, which is expected, not a bug.
- `packets.js` — **single source of truth for the launch data** (`PACKETS` + `LEDGER_ORDER`). Loaded by both pages. Add a launch here and it appears in the tool's matching AND the track record. Each packet has a `date` used by the ledger.
- `styles.css` — **shared design system** (tokens + components). Contains a few classes not currently used on any page (e.g. `.trio`, `.steps`, `.grid`, landing hero variants); leave them.

There is no build step, so the `?v=NN` cache-buster on each page's `<link rel="stylesheet" href="styles.css?v=NN">` is hand-maintained. **Any edit to `styles.css` must bump that number in both HTML files** (`index.html`, `track-record.html`) — otherwise a browser that already loaded the old `styles.css?v=NN` keeps serving it from cache and the change won't be visible until the number changes.

There is no `app.html` — an earlier two-page split (landing + tool) was reverted; the current split is instead tool vs. track-record.

This directory **is a git repository**, pushed to the `bharatlearner18-del/reality-filter` GitHub remote (see Deployment below) — `origin/HEAD` resolves and `git diff origin/HEAD...` works, so skills/tools that diff against it (e.g. `security-review`) no longer need the manual-diff fallback this file used to describe. Keep local in sync with `git pull --rebase origin main` before relying on that diff.

### Deployment
Live at **https://bharatlearner18-del.github.io/reality-filter/** — GitHub Pages, deployed from the public repo `bharatlearner18-del/reality-filter` (branch `main`) via the `.github/workflows/static.yml` GitHub Actions workflow (the "Static HTML" Pages template, not Jekyll — this site has no Jekyll front matter and would render wrong under it). Every push to `main` triggers a redeploy automatically, live in 1-2 minutes; no manual step after `git push`.
- **The user has two GitHub accounts** (`bharatlearner18-del`, the repo owner, and an older `bharattewatia463-cmd` that was cached in Windows' credential manager and caused a 403 on the first push). If a push is ever rejected with a permission error, that's almost certainly the cause — check which account the credential manager is using.
- `static.yml` was added through GitHub's web UI (the Pages setup flow), not from a local commit — so **local can silently drift behind the remote**. `git pull --rebase origin main` before starting new work, not just before pushing.
- Formspree (`forms.js`'s `FORMSPREE_ID`) should be domain-locked in the Formspree dashboard to the Pages host once it's live, or the public form ID can be spammed from other origins.

### Run / preview locally
`file://` is blocked in the in-app preview browser, so serve over HTTP:
```
python -m http.server 8731
# → http://localhost:8731/index.html
```
`.claude/launch.json` defines this as the `reality-filter` preview server, so the in-app Browser pane can start it via `preview_start`. Browser-pane CSS caching is aggressive during iteration — add a throwaway query param (`?v=…`) to force fresh loads.
There is no build, lint, or test step. "Testing" = drive the flow in a browser (see below). Note: automated clicks on the example chips can miss; calling `showResult('GPT-5.6')` in the console is a reliable way to verify rendering.

### Architecture (tool logic in `index.html`, data in `packets.js`)
- **`PACKETS` object (`packets.js`)** — the single source of truth for content. Each key is a filed launch with `match` (lowercase substring aliases used for lookup), `date` (for the ledger), the packet fields (`claim`, `stakes`, `found`, `missing`, `signals`, `pricing`, `next`), and the verdict (`verdict`, `verdictClass`, `fog`/`fogLabel`/`fogWhy` for the hype meter). To add or edit a packet, edit this file — both the tool and the track record pick it up. Add its key to `LEDGER_ORDER` to show it in the track record. **Every packet must follow the packet-writing standard** (see Content integrity below) — `stakes` is required, not optional; `renderPacket()` renders it as the "Why this matters" row and prints `undefined` if it's missing.
- **`findPacket(q)`** (in `index.html`) — matches loosely-phrased queries, not just exact aliases. Match → `renderPacket()` (evidence packet). No match → `renderCapture()` (email-capture panel). Two ways to hit:
  - **(a)** the query **contains** a whole alias verbatim — so `"heyy the new launch of chatgpt..."` matches a `chatgpt` alias.
  - **(b)** after stripping conversational filler (the `STOP` set — the/new/launch/tell/about/version/update/…), the query's remaining **content words equal an alias's content words as a set** — so `"gemini version 3"` matches alias `"gemini 3"` regardless of word order or filler.
  - **(b) is set-EQUALITY, not subset — that safety is the whole point.** Subset ("every alias word appears somewhere") would also match `"Gemini Ultra 3"` to a `"gemini 3"` packet, a *different product*, because a tier word looks like filler. Equality rejects it (`{gemini,ultra,3} ≠ {gemini,3}`). **`STOP` must hold only conversational filler — never product/tier words** (pro, ultra, mini, sol, turbo, numbers, or single letters that could be a token); adding one would let that word be stripped and cause a cross-tier mismatch.
  - **When filing a new packet, eyeball its aliases' content keys against existing packets'** (lowercase, strip `STOP`, dedupe, sort). If two packets share a content key, path (b) silently gives the one earlier in `PACKETS` — the same first-wins hazard as bare-word aliases. Current three don't collide.
  - **Miss rather than mismatch.** A fragment like `"ai"`, `"able"`, `"sales"`, or a bare brand word (`"gpt"`, `"claude"`) is neither contained as a whole alias nor equal to an alias's content set, so it still routes to the email capture — the demand signal we exist to collect. A wrong verdict costs trust, which is the moat; a miss we just answer by hand. When a real phrasing still misses, the fix is **adding an alias to `match`** in `packets.js` — that list is the control point. Do NOT add a bare `"gpt"`/`"claude"` alias to force bare-word matches: once a second GPT/Claude packet is filed, whichever sits earlier in `PACKETS` silently wins every such query (use a disambiguation UI then, not a broader matcher).
  - **One accepted edge:** path (a) is a plain substring, so `"gemini 3 pro"` still matches a `"gemini 3"` alias (it contains it verbatim), even though Pro may be a different tier. This is unchanged from the original substring matcher; `"Gemini Ultra 3"` is only safe because "ultra" sits mid-phrase and breaks the contiguous substring.
- **"Track this" CTA** (badge reads "Tracking" since the 2026-07-20 free repositioning) renders under the packet output — this is the per-launch tracking demand probe and must not be removed.
- Example chips and the `?q=` deep-link both call `showResult()`; the track-record rows (in `track-record.html`) deep-link into it via `index.html?q=<name>`.

### Forms → Formspree (`forms.js`)
The demand-signal forms (the no-match email capture, the tracking signup, and the "Track this" buttons) post through `sendSignal()` in **`forms.js`**, loaded by `index.html`. (The feedback form/page was removed 2026-07-23 — see below.)
- **`FORMSPREE_ID` at the top of `forms.js` is the only thing to set.** Paste the ID from `https://formspree.io/f/XXXXXXXX`.
- **While it's empty every form stays a stub**: `sendSignal()` returns `{ok:true, skipped:true}`, so the success state shows and nothing is sent. This is deliberate — the design handoff shipped a hardcoded `YOUR_FORM_ID` placeholder that made *every* submit fail with an error, and this is the guard against repeating that. An unconfigured site must never show users an error.
- All post to the **same endpoint**; the `type` field (`packet request` / `tracking signup` / `track this`) tells them apart in the inbox, which keeps it inside Formspree's free single-form tier. `page` is added automatically. (`tracking signup` was called `premium waitlist` before the 2026-07-20 free repositioning — old inbox rows may carry the old label.)
- The tracking-signup signal also carries **`watching`** — the topic chips the user picked (comma-joined). The submit handler requires ≥1 chip before sending (no chip → `.wl-hint` shows and nothing is sent), so every signup row in the inbox says WHAT that person wants tracked. This is deliberate signal-quality data (2026-07-20, after zero signups on the email-only version) — don't make the chips optional without asking.
- On a real send failure (network down, 4xx) `sendSignal` returns `{ok:false}` and the handler re-enables the form and shows `.form-err` — never a silent success, since the user believes they're on the list.
- The "Track this" CTA has no email field, so its signal is an anonymous click count (launch name only). If follow-up matters, that needs a product change, not a wiring one.

### Still deliberately stubbed (validation build)
- Packet content is hardcoded in `packets.js`, not fetched. Concierge delivery (manual Gmail) is still the plan — see the validation plan in `research-brief.md`.

## Design system (keep consistent when editing UI)
Concept: a warm parchment-canvas editorial shell modeled on **Claude/Anthropic's** product design language (this replaced an earlier Cursor-style reskin — cream canvas, sans-only Inter, hairline-only depth, orange accent — which the user deliberately abandoned in favor of this look; do not revert to the Cursor system described in old history without being asked). The pairing is **serif headlines + sans body/UI**: `.display`, `.packet-name`, `.section h2`, `.ht-card-title`, `.for-card h3`, and `.capture h3` all use the book-serif `--font-serif` at weight 400 with only slight negative tracking (serif tracks badly with tight negative letter-spacing — don't push it past about `-.01em`); everything else (body copy, nav, buttons, mono labels) stays on Inter/JetBrains Mono. Depth is **soft shadow, not hairline**: outer cards (`.packet`, `.capture`, `.ledger`, `.ht-card`, `.for-card`) use `--shadow-card` instead of a `border`; hairlines (`--hairline`/`--hairline-strong`) are kept only for *internal* dividers (packet rows, ledger rows, inbox/persona list rows). Signature elements unchanged in role: the white evidence card and the hype-risk "fog meter" (the old ink-inverted Premium band was removed in the 2026-07-20 redesign).
- Fonts: `Source Serif 4` (`--font-serif`, headlines only), `Inter` (`--font-sans`, body/UI), `JetBrains Mono` (`--font-mono`, every label/data/caption/ledger surface). All three loaded via the Google Fonts `<link>` in both HTML pages — if you add a font weight/style, update that link, not just the CSS var.
- Surfaces: `--canvas` (#f0eee6 warm parchment page floor) → `--surface-card` (#fff white cards: packet, capture, ledger, chips, inputs) → `--surface-strong`/`--canvas-soft` (soft fills) → `--ink` (#3d3929, the darkest text tone; no surface uses it since the ink-inverted Premium band was removed 2026-07-20). Outer cards float via `--shadow-card`; hairlines still separate rows *within* a card.
- **Claude terracotta (`--primary` #d97757) is the single brand voltage and stays scarce** — only the true conversion CTAs (`.btn` search/capture/waitlist), the `.real-word` hero accent, and the `.shine` glint (below). The nav is otherwise plain text links (no button), which keeps the accent rare and avoids a redundant CTA next to the search bar. Don't paint eyebrows, labels, or borders terracotta as decoration.
- **`.toplink.shine`** marks the demand-signal links we most want acted on. It's currently unused in markup — its last uses were the nav **Feedback** link and the footer "Give us your feedback →" CTA, both removed 2026-07-23 along with `feedback.html`. The CSS rule was left in place (not a hardcoded color, still token-driven) for reuse on a future demand-signal link — the text stays `--ink` (bold, AA-legible) while a terracotta+light glint sweeps across it via `background-clip:text` — deliberately NOT solid terracotta, because terracotta text on cream is only ~2.7:1 and fails AA at nav size. Under `prefers-reduced-motion` it falls back to solid bold `--ink`. Any hardcoded `rgba(...)` shadow/border tied to the accent (focus rings, badge borders) must use the terracotta RGB (217,119,87), not the old orange (245,78,0) — grep for `245,78,0` if you see an orange-tinted ring after a color-token change.
- Small mono labels on cream/white use `--label` (#6b6552), a tone tuned to clear ~4.5:1 at caption size; `--muted`/`--muted-soft` are for larger or secondary text only (they fail AA at 11–13px).
- **AI-timeline pastels** (`--tl-thinking/grep/read/edit/done` — peach/mint/blue/lavender/gold): scoped to the evidence packet's row dots as "investigation stages," driven by `:nth-child` on the fixed `renderPacket()` row order (1 claim=peach · 2 why-this-matters=peach · 5 experts=blue · 6 pricing=lavender · 7 meter=mint · 8 next=gold; rows 3/4 found/miss keep semantic green/red). The nth-child selectors are MORE specific than `.row.found`/`.row.miss`, so no nth-child dot rule may target rows 3/4 or it silently overrides the semantic green/red. If you change the row order in `renderPacket()`, update those `nth-child` targets in `styles.css` (both the dot colors and the row animation delays, which run 1–8).
- Verdict colors are a 4-point scale via CSS vars/classes, darkened to read on white/cream: `v-go` (green), `v-watch` (gold), `v-early` (blue), `v-fog` (red/pink). Use these, not one-off colors.
- All colors/spacing come from CSS custom properties in `:root`. Derive new styles from those tokens.
- **Dark mode** is a manual toggle (not `prefers-color-scheme`): the `.theme-toggle` button in the nav flips a `dark` class on `<html>` (so the selector everywhere is `html.dark`, not a bare `.dark`), and the choice persists in `localStorage` under `rf-theme`. Every page carries a tiny inline script in `<head>` that re-applies the saved class *before paint* to avoid a flash of the light theme — if you add a page, copy that script or it will flash. Dark styling is done by overriding the same `:root` tokens under `html.dark`, so new components theme themselves for free **as long as you use the tokens** rather than hardcoding a hex. The `html.dark` block near the top of `styles.css` is the exception that proves the rule: a gradient stop there (`#343128` on `.track-cta`) is a bespoke tint with no token equivalent. Leave it, but don't add more — if you need a dark-only color that a token already covers, use the token.
- **`.track-cta`** (the "Track this" strip under the evidence packet) is its own soft `--canvas-soft` block with a small gap above it, not fused to the packet's bottom edge — that fused-border trick only worked when `.packet` had a hairline border, which it no longer does now that packets use `--shadow-card`.
- **Signup card + how-it-works demo** (the old "Premium band" — dissolved into the landing redesign 2026-07-20): the signup lives in the hero as `.ht-card` (see the `index.html` bullet), and **`.p-demo`** — the 4-frame auto-advancing clip (you pick → we re-check → verdict changes → one email) — lives in `#how` beside the numbered steps. The `.p-*` and `.wl-*` selectors are now **bare/global** (the old `.premium`/`.waitlist` scoping was removed with the band — don't re-scope them under a container that no longer exists). `@keyframes pulse` survived the band's deletion because `.free-badge`/`.live-dot` still use it (`spinSlow` was only used by the now-removed `.fb-inpage` decorative rings and was deleted with it 2026-07-23). The demo's story is the **real GPT-5.6 re-check** (filed "Too early" Jul 14 → upgraded Jul 18 when independent benchmark numbers landed — GA rollout had already begun Jul 9, so don't phrase the upgrade as "GA arrived") — but the frames **hand-duplicate `packets.js`'s `gpt56` entry** rather than reading from it, an accepted exception to the single-source-of-truth rule (static markup keeps the demo simple). The cost: **any future gpt56 re-check must also update these frames** or the demo will misstate the record on the very page that sells accountability. Demo mechanics: frames are grid-stacked (`grid-area:1/1`) so the card auto-sizes to the tallest frame — no hardcoded heights; `initHowDemo()` in `index.html` drives a 3.2s `setInterval` that runs only while the demo is on screen (IntersectionObserver), pauses on hover/keyboard focus, and never auto-advances under `prefers-reduced-motion` (the dots remain manual controls). Like the trStats count-up, the loop appears frozen in a hidden/background tab because timers are throttled — expected, not a bug. The chips are the demand-signal core: selected topics ride along in the signup signal as `watching` (see Forms above).
- **Footer (both pages)** is a single `.fwrap` row: the brand blurb (`Reality Filter` blurb left, `See the track record →` right on `index.html`, back-link on `track-record.html`). The `.fb-row` feedback CTA row and the `feedback.html` page it linked to were both removed 2026-07-23 — don't re-add without being asked. Below the row sits `.disc`, the fine print — on `index.html` it also carries the **Formspree disclosure** (the page that actually collects data); `track-record.html` collects nothing so its `.disc` stays the short version. `.disc` sets `max-width:60ch` to override `footer p`'s 46ch, which at 11px mono is only ~304px and wrapped the disclosure into four cramped lines.
- **Mobile (≤720px) nav**: brand left, the nav links (Check a launch · Track record · Start tracking) plus the theme toggle right-aligned. When they don't fit beside the brand the `.topbar .wrap` flex-wraps them onto a second row under it (topbar grows to fit); the brand never wraps. Don't hide `.nav-center` — those links are the only route to the track record on phones. Other mobile rules that look odd but are load-bearing: `.btn` gets real vertical padding (it collapses to ~20px tall when it wraps off the input's flex line), and `.ledger-row .vd` allows wrapping so long verdicts ("Worth trying — with caveats") stay inside the ledger card.

## Automated review (two subagents, auto-triggered)
After any code change, TWO review subagents run automatically to check it before the user sees it:
- **`code-reviewer`** (`.claude/agents/code-reviewer.md`) — bugs and quality issues.
- **`security-reviewer`** (`.claude/agents/security-reviewer.md`) — XSS, injection, data leaks, risky third-party loads.

Both load the `find-skills` skill first, are read-only (report, don't edit), and run in parallel. Wiring is in `.claude/settings.json`: a `PostToolUse` hook flags `.claude/.review-pending` when a `.html`/`.css`/`.js` file is edited, and the `Stop` hook injects a reminder to dispatch both reviewers before the turn ends (then clears the flag). To pause, remove the `Stop` hook from `.claude/settings.json`. Note: after editing `.claude/settings.json` you may need to reload config (`/hooks` or restart) for changes to take effect.

## Content integrity
Verdicts are the product's only asset — accuracy is the whole point. Packet content must be real and evidence-based (researched from current sources), never invented placeholder data. A wrong "worth trying now" damages trust, which is the moat.

### Packet-writing standard (product behavior, not a one-off)
Set with the user 2026-07-18: a packet that only lists facts is a failed packet — the reader must feel why it matters to *them*. Every packet (new or re-checked) must:
- **Open with stakes.** The `stakes` field ("Why this matters" row) addresses the reader directly — founder, builder, buyer — and says what decision this verdict changes for them. Second person, concrete ("this decides whether you re-run your evals this month"), never generic ("this is an important development").
- **Name the evidence.** Numbers with sources in the text itself (benchmark names + scores, filings, named reviewers like Simon Willison / METR / The Verge), not vague "reports suggest." Research fresh at filing time — the WebSearch/WebFetch step is mandatory, per the rule above.
- **Separate signal from hype explicitly.** `found` = what's independently verified; `missing` = where the official claim outruns the evidence; `signals` (rendered as "Experts & real users") = what named experts, real users, and companies actually say/do — including evidence *against* our own verdict when it exists (both-sides candor is what makes the verdict credible).
- **Segment the next step by reader.** `next` gives different advice to builders vs buyers vs everyone else, not one generic recommendation.
- **Re-check, never quietly edit.** When facts change, update the packet AND its `date`, and state the change inside the packet body (e.g. `found` opens with "Updated 2026-07-18 — we filed this 'Too early' on Jul 14…"). Verdict changes are allowed and are the product working — GPT-5.6 went "Too early" → "Worth trying — with caveats" when GA arrived; the old call stays acknowledged on the record. The `.ledger-note` on track-record.html hand-carries the latest re-check date — update it too.
